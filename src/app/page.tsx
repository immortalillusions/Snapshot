"use client";

import { useEffect, useState } from "react";
import { Bell, CalendarDays, Check, ChevronDown, CircleHelp, Clock3, Filter, GripVertical, Link2, Pencil, Plus, Settings2, Sparkles, Trash2, X } from "lucide-react";

type Task = { id: string; name: string; course: string; due_at: string; completed: boolean; color: string };
type Course = { id: string; name: string; position: number };
const orderCourses = (courses: Course[], courseOrder: string[]) => [...courses].sort((left, right) => { const leftIndex = courseOrder.indexOf(left.name); const rightIndex = courseOrder.indexOf(right.name); return (leftIndex < 0 ? Number.MAX_SAFE_INTEGER : leftIndex) - (rightIndex < 0 ? Number.MAX_SAFE_INTEGER : rightIndex) || left.name.localeCompare(right.name); });
const demoTasks: Task[] = [
  { id: "demo-1", name: "Read chapter 6 + notes", course: "CS 2214", due_at: "2026-09-02T23:59:00", completed: false, color: "coral" }, { id: "demo-2", name: "Problem set 4", course: "MATH 2210", due_at: "2026-09-03T09:00:00", completed: false, color: "blue" }, { id: "demo-3", name: "Lab report: motion", course: "PHYS 1020", due_at: "2026-09-06T14:00:00", completed: false, color: "green" }, { id: "demo-4", name: "Midterm review sheet", course: "CS 2214", due_at: "2026-09-08T23:59:00", completed: false, color: "coral" }, { id: "demo-5", name: "Essay outline", course: "ENGL 1100", due_at: "2026-09-11T23:59:00", completed: false, color: "yellow" }, { id: "demo-6", name: "Quiz 2", course: "MATH 2210", due_at: "2026-09-12T08:30:00", completed: true, color: "blue" },
];
const colors = ["coral", "blue", "green", "yellow"];
const dateLabel = (value: string) => new Intl.DateTimeFormat("en", { month: "short", day: "numeric" }).format(new Date(value));
const timeLabel = (value: string) => new Intl.DateTimeFormat("en", { hour: "numeric", minute: "2-digit" }).format(new Date(value));

export default function Home() {
  const [tasks, setTasks] = useState<Task[]>(demoTasks); const [courses, setCourses] = useState<Course[]>([]); const [activeTab, setActiveTab] = useState("Upcoming"); const [connected, setConnected] = useState(false); const [showAdd, setShowAdd] = useState(false); const [showSettings, setShowSettings] = useState(false); const [showCourse, setShowCourse] = useState(false); const [editing, setEditing] = useState<Task | null>(null);
  const [form, setForm] = useState({ name: "", course: "CS 2214", dueAt: "2026-09-02T23:59", completed: false }); const [settings, setSettings] = useState({ summaryStartTime: "09:30", summaryDurationMinutes: 30, lookaheadDays: 10, minimumPerCourse: 2, courseOrder: [] as string[] }); const [draggedCourse, setDraggedCourse] = useState<string | null>(null); const [isProcessing, setIsProcessing] = useState(false);
  const load = async () => { const response = await fetch("/api/tasks"); if (!response.ok) return; setConnected(true); const rows = await response.json(); setTasks(rows.map((row: Task, i: number) => ({ ...row, color: colors[i % colors.length] }))); const cr = await fetch("/api/courses"); const courseRows = cr.ok ? await cr.json() as Course[] : []; const sr = await fetch("/api/settings"); const nextSettings = sr.ok ? await sr.json() : { courseOrder: [] }; setCourses(orderCourses(courseRows, nextSettings.courseOrder ?? [])); if (sr.ok) setSettings(nextSettings); };
  // Initial hydration synchronizes the client with the authenticated API state.
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { void load(); }, []);
  useEffect(() => { if (isProcessing) document.body.dataset.processing = "true"; else delete document.body.dataset.processing; return () => { delete document.body.dataset.processing; }; }, [isProcessing]);
  const runPending = async <T,>(work: () => Promise<T>) => { if (isProcessing) return; setIsProcessing(true); try { return await work(); } finally { setIsProcessing(false); } };
  const orderedCourseNames = [...settings.courseOrder, ...courses.map(course => course.name), ...tasks.map(task => task.course)].filter((course, index, all) => course && all.indexOf(course) === index);
  const reorderCourse = (source: string, target: string) => { if (source === target) return; const order = [...orderedCourseNames]; const sourceIndex = order.indexOf(source); const targetIndex = order.indexOf(target); if (sourceIndex < 0 || targetIndex < 0) return; order.splice(sourceIndex, 1); order.splice(targetIndex, 0, source); setSettings(current => ({ ...current, courseOrder: order })); };
  const visible = tasks.filter(task => activeTab === "Completed" ? task.completed : !task.completed); const groupedByCourse = visible.reduce<Record<string, Task[]>>((all, task) => { (all[task.course] ||= []).push(task); return all; }, {}); const orderedGroups = [...orderedCourseNames, ...Object.keys(groupedByCourse).filter(course => !orderedCourseNames.includes(course))].flatMap(course => groupedByCourse[course] ? [[course, groupedByCourse[course]] as const] : []); const grouped = Object.fromEntries(orderedGroups);
  const update = async (task: Task) => { if (task.id.startsWith("demo-")) { setTasks(current => current.map(item => item.id === task.id ? task : item)); return; } await runPending(async () => { await fetch(`/api/tasks/${task.id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: task.name, course: task.course, dueAt: task.due_at, completed: task.completed }) }); await load(); }); };
  const openEditor = (task?: Task) => { setEditing(task ?? null); setForm(task ? { name: task.name, course: task.course, dueAt: task.due_at.slice(0, 16), completed: task.completed } : { name: "", course: orderedCourseNames[0] ?? "CS 2214", dueAt: "2026-09-02T23:59", completed: false }); setShowAdd(true); };
  const saveTask = async () => { if (!form.name.trim()) return; if (editing) await update({ ...editing, name: form.name.trim(), course: form.course, due_at: form.dueAt, completed: form.completed }); else await runPending(async () => { const response = await fetch("/api/tasks", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: form.name, course: form.course, dueAt: new Date(form.dueAt).toISOString(), completed: form.completed }) }); if (response.ok) await load(); else setTasks(current => [...current, { id: `demo-${Date.now()}`, name: form.name, course: form.course, due_at: form.dueAt, completed: form.completed, color: "coral" }]); }); setShowAdd(false); setEditing(null); };
  const remove = async (task: Task) => { if (task.id.startsWith("demo-")) setTasks(current => current.filter(item => item.id !== task.id)); else await runPending(async () => { await fetch(`/api/tasks/${task.id}`, { method: "DELETE" }); await load(); }); };
  const addCourse = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const name = String(new FormData(event.currentTarget).get("name") || "").trim(); if (!name) return; await runPending(async () => { const response = await fetch("/api/courses", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name }) }); if (response.ok) await load(); setShowCourse(false); }); };
  const saveSettings = async () => { await runPending(async () => { const response = await fetch("/api/settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(settings) }); if (response.ok) { const nextSettings = await response.json(); setSettings(nextSettings); setCourses(current => orderCourses(current, nextSettings.courseOrder ?? [])); setShowSettings(false); } }); };

  return <main className="shell"><aside className="sidebar"><div className="brand"><span className="brand-mark"><Sparkles size={17} /></span><span>snapshot</span></div><div className="workspace-label">WORKSPACE</div><nav className="nav"><button className="nav-item active"><CalendarDays size={18} /> Overview</button><button className="nav-item"><Check size={18} /> All tasks <span className="nav-count">{tasks.length}</span></button></nav><div className="workspace-label courses-label">COURSES <button aria-label="Add course" onClick={() => setShowCourse(true)}><Plus size={15} /></button></div><div className="course-list">{(courses.length ? courses : [...new Set(tasks.map(task => task.course))].map((name, i) => ({ id: name, name, position: i }))).map(course => <span key={course.id}><i className={`dot ${colors[course.position % colors.length]}`} /> {course.name}</span>)}</div><div className="sidebar-bottom"><button className="nav-item" onClick={() => setShowSettings(true)}><Settings2 size={18} /> Settings</button><button className="nav-item"><CircleHelp size={18} /> Help center</button><div className="profile"><div className="avatar">JL</div><div><strong>Jordan Lee</strong><small>{connected ? "Calendar connected" : "Demo workspace"}</small></div></div></div></aside>
    <section className="content"><header className="topbar"><div className="mobile-brand">snapshot</div><div className="sync-status"><span className="pulse" /> {connected ? "Synced with Google Calendar" : "Preview mode"} <span className="sync-time">{connected ? "just now" : "connect to sync"}</span></div><div className="top-actions"><button className="icon-button" aria-label="Notifications"><Bell size={18} /></button>{connected ? <button className="settings-link" onClick={() => setShowSettings(true)}><Settings2 size={17} /> Settings</button> : <a className="settings-link" href="/api/auth/google"><Link2 size={17} /> Connect Google</a>}</div></header><div className="page-heading"><div><p className="eyebrow">WEDNESDAY, SEPTEMBER 2, 2026</p><h1>Your day, in focus.</h1><p className="lede">Here&apos;s what needs your attention next.</p></div><button className="add-button" onClick={() => openEditor()}><Plus size={18} /> Add task</button></div><div className="summary-strip"><div><span className="summary-label">UP NEXT</span><strong>{tasks.filter(task => !task.completed && task.due_at.slice(0, 10) === "2026-09-02").length} <small>tasks today</small></strong></div><div className="strip-divider" /><div><span className="summary-label">THIS WEEK</span><strong>{tasks.filter(task => !task.completed).length} <small>tasks remaining</small></strong></div><div className="strip-spacer" /><div className="calendar-chip"><Link2 size={16} /><span>{connected ? "Calendar connected" : "Demo data"}</span></div></div><div className="task-toolbar"><div className="tabs">{["Upcoming", "Completed"].map(tab => <button key={tab} className={activeTab === tab ? "tab active-tab" : "tab"} onClick={() => setActiveTab(tab)}>{tab}<span>{tab === "Upcoming" ? tasks.filter(t => !t.completed).length : tasks.filter(t => t.completed).length}</span></button>)}</div><button className="filter-button"><Filter size={15} /> All courses <ChevronDown size={14} /></button></div><div className="task-area">{Object.entries(grouped).map(([course, courseTasks]) => <section className="course-section" key={course}><div className="course-heading"><div className={`course-line ${courseTasks[0].color}`} /><h2>{course}</h2><span>{courseTasks.length} {courseTasks.length === 1 ? "task" : "tasks"}</span></div><div className="task-list">{courseTasks.map(task => <article className={task.completed ? "task completed" : "task"} key={task.id}><button className="task-check" onClick={() => void update({ ...task, completed: !task.completed })} aria-label={task.completed ? "Mark incomplete" : "Mark complete"}>{task.completed && <Check size={14} />}</button><div className="task-info"><strong>{task.name}</strong><span><Clock3 size={13} /> {dateLabel(task.due_at)} <b>·</b> {timeLabel(task.due_at)}</span></div><button className="task-more" onClick={() => openEditor(task)} aria-label={`Edit ${task.name}`}><Pencil size={14} /></button><button className="task-more" onClick={() => void remove(task)} aria-label={`Delete ${task.name}`}><Trash2 size={14} /></button></article>)}</div></section>)}{!visible.length && <div className="empty-state"><Check size={25} /><h2>All caught up</h2><p>Nothing else is waiting for you here.</p></div>}</div><footer className="content-footer"><span><span className="footer-dot" /> {connected ? "Summary events update automatically" : "Preview data only"}</span><button><CalendarDays size={14} /> View calendar</button></footer></section>
    {showAdd && <div className="modal-backdrop" onClick={() => setShowAdd(false)}><form className="modal" onClick={event => event.stopPropagation()} onSubmit={event => { event.preventDefault(); void saveTask(); }}><div className="modal-head"><div><span className="eyebrow">{editing ? "EDIT TASK" : "NEW TASK"}</span><h2>{editing ? "Edit task" : "Add a task"}</h2></div><button type="button" onClick={() => setShowAdd(false)} aria-label="Close"><X size={19} /></button></div><label>Task name<input value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} placeholder="e.g. Finish reading" autoFocus /></label><label>Course<select value={form.course} onChange={event => setForm({ ...form, course: event.target.value })}>{[...new Set([...courses.map(course => course.name), ...tasks.map(task => task.course)])].map(course => <option key={course}>{course}</option>)}</select></label><label>Due date and time<input type="datetime-local" value={form.dueAt} onChange={event => setForm({ ...form, dueAt: event.target.value })} /></label><label className="check-label"><input type="checkbox" checked={form.completed} onChange={event => setForm({ ...form, completed: event.target.checked })} /> Completed</label><button className="add-button full" type="submit">{editing ? "Save changes" : "Create task"} <Check size={17} /></button></form></div>}
    {showCourse && <div className="modal-backdrop" onClick={() => setShowCourse(false)}><form className="modal" onClick={event => event.stopPropagation()} onSubmit={addCourse}><div className="modal-head"><div><span className="eyebrow">COURSES</span><h2>New course</h2></div><button type="button" onClick={() => setShowCourse(false)} aria-label="Close"><X size={19} /></button></div><label>Course name<input name="name" placeholder="e.g. HIST 1100" autoFocus /></label><button className="add-button full" type="submit">Add course <Plus size={17} /></button></form></div>}
    {showSettings && <div className="modal-backdrop" onClick={() => setShowSettings(false)}><div className="modal settings-modal" onClick={event => event.stopPropagation()}><div className="modal-head"><div><span className="eyebrow">PREFERENCES</span><h2>Snapshot settings</h2></div><button onClick={() => setShowSettings(false)} aria-label="Close"><X size={19} /></button></div><div className="setting-row"><div><strong>Summary event</strong><span>Start time</span></div><input type="time" value={settings.summaryStartTime} onChange={event => setSettings({ ...settings, summaryStartTime: event.target.value })} /></div><div className="setting-row"><div><strong>Event duration</strong><span>Minutes</span></div><input type="number" min="5" step="5" value={settings.summaryDurationMinutes} onChange={event => setSettings({ ...settings, summaryDurationMinutes: Number(event.target.value) })} /></div><div className="setting-row"><div><strong>Lookahead window</strong><span>Calendar days included</span></div><select value={settings.lookaheadDays} onChange={event => setSettings({ ...settings, lookaheadDays: Number(event.target.value) })}><option value={7}>7 days</option><option value={10}>10 days</option><option value={14}>14 days</option></select></div><div className="setting-row"><div><strong>Minimum per course</strong><span>Keep each course visible</span></div><select value={settings.minimumPerCourse} onChange={event => setSettings({ ...settings, minimumPerCourse: Number(event.target.value) })}><option value={1}>1 task</option><option value={2}>2 tasks</option><option value={3}>3 tasks</option></select></div><div className="setting-row course-order-row"><div><strong>Course order</strong><span>Drag courses to reorder summary sections</span></div><div className="course-order" aria-label="Course order">{orderedCourseNames.map(course => <div key={course} className={draggedCourse === course ? "course-tile dragging" : "course-tile"} draggable onDragStart={event => { event.dataTransfer.effectAllowed = "move"; event.dataTransfer.setData("text/plain", course); setDraggedCourse(course); }} onDragOver={event => event.preventDefault()} onDrop={event => { event.preventDefault(); reorderCourse(event.dataTransfer.getData("text/plain") || draggedCourse || "", course); setDraggedCourse(null); }} onDragEnd={() => setDraggedCourse(null)}><GripVertical size={15} aria-hidden="true" /><span>{course}</span></div>)}</div></div><button className="add-button full" onClick={() => void saveSettings()}>Save preferences <Check size={17} /></button></div></div>}
  </main>;
}
